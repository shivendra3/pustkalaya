<%= form_for(issue) do |f| %>
  <% if issue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(issue.errors.count, "error") %> prohibited this issue from being saved:</h2>

      <ul>
      <% issue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sid %>
    <%= f.text_field :sid %>
  </div>

  <div class="field">
    <%= f.label :totalbook_id %>
    <%= f.number_field :totalbook_id %>
  </div>

  <div class="field">
    <%= f.label :issue_date %>
    <%= f.date_select :issue_date %>
  </div>

  <div class="field">
    <%= f.label :return_date %>
    <%= f.date_select :return_date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
